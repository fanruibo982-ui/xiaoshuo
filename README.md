# 自动写小说阅读器

一个开箱即用的静态网页：输入题材、主角和章节长度后，可以手动或自动生成章节，形成“边写边读”的体验。

## 使用方式

1. 直接打开 `index.html`。
2. 填写参数后点击 `生成下一章`。
3. 点击 `自动续写` 可每 4 秒续写一章。
4. 点击 `清空` 可重置阅读内容。

## 本地启动（可选）

```bash
python3 -m http.server 8000
```

然后访问 <http://localhost:8000>。
